import { useState } from "react";

export function HandlePokemonId(pokeId: number) {
    
    // const [ pokemonInfoId, setPokemonInfoId ] = useState<number>(0);

    // setPokemonInfoId(pokeId)

    // console.log(pokemonInfoId, 'teste1')

    // return pokemonInfoId;
}